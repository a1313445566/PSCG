# 2026-04-07 工作日志

## Phase 1.3 尺寸变量补齐 + 方案文档修订

- **_variables.scss 新增 20 个尺寸变量**：
  - 间距 5 个：`$spacing-2xs`(2px), `$spacing-3xl`(64px), `$spacing-compact`(12px), `$spacing-comfortable`(20px), `$spacing-section`(40px)
  - 字号 9 个：`$font-size-xs`(12px) ~ `$font-size-4xl`(32px)
  - 圆角 3 个：`$border-radius-xs`(4px), `$border-radius-xl`(32px), `$border-radius-full`(50px)
  - 边框宽度 3 个：`$border-width`(1px), `$border-width-md`(2px), `$border-width-lg`(3px)
- **命名决策**：`$spacing-md` 保持 16px（76+ 处已引用），用 `$spacing-compact: 12px` 代替
- 所有变量均已同步写入 `:root` CSS 自定义属性

- **文档修订**（v5.1 / v2.1）：
  - 总览文档新增「变量体系总览」章节，包含完整的间距/字号/圆角/边框宽度替换速查表
  - 清理了总览文档中的 git merge conflict markers（大量旧版本冲突内容）
  - 三个文档同步新增 Phase 1.3、更新时间线、更新版本历史
  - 改造范围从纯颜色扩展为颜色 + 尺寸（~1575 颜色 + ~700+ 尺寸）
  - AI Guard Rails 新增：尺寸变量保守原则、固定尺寸/动画偏移量不替换

- **硬编码统计**（尺寸类）：
  - SCSS 中 ~416 处 px 硬编码（`src/styles/scss/`）
  - Vue 组件中 ~950+ 处 px 硬编码（`src/components/`）
  - 间距 ~350 处、字号 ~264 处、圆角 ~92 处、边框宽度 ~25 处
