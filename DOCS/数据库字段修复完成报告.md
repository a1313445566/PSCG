# 数据库字段修复完成报告

**修复时间**: 2026-03-31
**修复范围**: AI 聊天系统相关服务文件
**修复原则**: 修改代码适配数据库表结构

---

## ✅ 已修复文件

### 1. `services/chat/cacheService.js`

**问题**:
- ❌ 使用不存在的 `response_json` 字段

**修复**:
- 移除所有 `response_json` 相关代码
- `cache_hits` 表仅用于统计，不存储响应内容
- 响应内容仅保存在内存缓存中

**修改前**:
```sql
SELECT response_json, tokens_saved
FROM cache_hits
```

**修改后**:
```sql
SELECT query_hash, hit_count, tokens_saved
FROM cache_hits
```

---

### 2. `services/chat/chatService.js`

**问题**:
- ❌ 表名错误：`ai_chat_sessions` → `chat_sessions`
- ❌ 表名错误：`ai_chat_history` → `chat_messages`
- ❌ 字段名错误：`model_id` → `model_name`（chat_sessions 表）
- ❌ 字段名错误：`last_message_at` → `updated_at`
- ❌ 字段名错误：`tool_calls` → `tools_used`
- ❌ 字段名错误：`tokens_used` → `tokens`
- ❌ 字段名错误：`model_name` → `model_id`（token_usage 表）

**修复**:

| 原字段名 | 修正后字段名 | 所在表 |
|---------|-------------|--------|
| `ai_chat_sessions` | `chat_sessions` | 表名 |
| `ai_chat_history` | `chat_messages` | 表名 |
| `model_id` | `model_name` | chat_sessions |
| `last_message_at` | `updated_at` | chat_sessions |
| `tool_calls` | `tools_used` | chat_messages |
| `tokens_used` | `tokens` | chat_messages |
| `model_name` | `model_id` | token_usage |

**关键修改示例**:

```javascript
// 修改前
INSERT INTO ai_chat_sessions (id, admin_id, title, model_id, message_count, last_message_at)

// 修改后
INSERT INTO chat_sessions (id, admin_id, title, model_name, message_count, updated_at)
```

```javascript
// token_usage 表（修改前）
INSERT INTO token_usage (admin_id, session_id, model_name, ...)

// token_usage 表（修改后）
INSERT INTO token_usage (admin_id, session_id, model_id, ...)
```

---

### 3. `services/chat/modelManager.js`

**问题**:
- ❌ 字段名错误：`base_url` → `api_url`
- ❌ 字段名错误：`is_default` → `is_primary`
- ❌ 字段名错误：`is_active` → `is_enabled`
- ❌ 字段名错误：`api_key` → `api_key_encrypted`

**修复**:

| 原字段名 | 修正后字段名 | 所在表 |
|---------|-------------|--------|
| `base_url` | `api_url` | ai_models |
| `is_default` | `is_primary` | ai_models |
| `is_active` | `is_enabled` | ai_models |
| `api_key` | `api_key_encrypted` | ai_models |

**关键修改示例**:

```javascript
// 修改前
SELECT id, name, provider, base_url, model_id, is_default, is_active
FROM ai_models

// 修改后
SELECT id, name, provider, api_url as base_url, model_id, is_primary as is_default, is_enabled as is_active
FROM ai_models
```

**说明**: 为了保持前端接口兼容性，使用 `AS` 别名映射到原来的字段名。

---

## 📊 数据库表结构参考

### `ai_models` 表
```sql
CREATE TABLE ai_models (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(50) NOT NULL COMMENT '模型名称',
  provider VARCHAR(50) NOT NULL COMMENT '提供商',
  model_id VARCHAR(100) NOT NULL COMMENT '模型标识',
  api_url VARCHAR(255) NOT NULL COMMENT 'API 地址',
  api_key_encrypted TEXT COMMENT '加密后的 API Key',
  is_primary TINYINT(1) DEFAULT 0 COMMENT '是否主力模型',
  is_enabled TINYINT(1) DEFAULT 1 COMMENT '是否启用',
  priority INT DEFAULT 0,
  max_tokens INT DEFAULT 4096,
  temperature DECIMAL(3,2) DEFAULT 0.7,
  config JSON,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
)
```

### `chat_sessions` 表
```sql
CREATE TABLE chat_sessions (
  id VARCHAR(36) PRIMARY KEY,
  admin_id INT NOT NULL,
  title VARCHAR(100),
  model_name VARCHAR(50),
  total_tokens INT DEFAULT 0,
  total_cost DECIMAL(10,4) DEFAULT 0,
  message_count INT DEFAULT 0,
  status ENUM('active', 'archived') DEFAULT 'active',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  expires_at DATETIME,
  FOREIGN KEY (admin_id) REFERENCES admin_credentials(id)
)
```

### `chat_messages` 表
```sql
CREATE TABLE chat_messages (
  id INT PRIMARY KEY AUTO_INCREMENT,
  session_id VARCHAR(36) NOT NULL,
  role ENUM('user', 'assistant', 'system') NOT NULL,
  content TEXT NOT NULL,
  tokens INT DEFAULT 0,
  tools_used JSON,
  query_result JSON,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (session_id) REFERENCES chat_sessions(id)
)
```

### `cache_hits` 表
```sql
CREATE TABLE cache_hits (
  id INT PRIMARY KEY AUTO_INCREMENT,
  query_hash VARCHAR(64) NOT NULL,
  query_text TEXT NOT NULL,
  response_hash VARCHAR(64) NOT NULL,
  hit_count INT DEFAULT 1,
  tokens_saved INT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  last_hit_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
)
```

---

## 🔍 未修改文件

以下文件**无需修改**，因为它们通过服务层访问数据库，不直接使用字段名：

- ✅ `routes/chat.js` - 仅调用服务层方法
- ✅ `services/chat/agent.js` - 仅调用工具和模型管理器
- ✅ `services/chat/contextCompressor.js` - 不涉及数据库操作

---

## ✅ 验证清单

修复后请验证以下功能：

### 1. 模型管理
- [ ] 获取模型列表
- [ ] 添加新模型
- [ ] 更新模型配置
- [ ] 测试模型连接
- [ ] 设置默认模型
- [ ] 删除模型

### 2. 会话管理
- [ ] 创建新会话
- [ ] 获取会话列表
- [ ] 获取历史消息
- [ ] 删除会话

### 3. AI 对话
- [ ] 发送消息并获取流式响应
- [ ] Function Calling 工具调用
- [ ] Token 使用统计
- [ ] 缓存命中统计

### 4. 数据库操作
- [ ] 所有 SQL 查询正常执行
- [ ] 无字段名错误
- [ ] 无表名错误
- [ ] 数据正确保存和读取

---

## 📝 注意事项

1. **前端接口兼容性**: modelManager.js 使用 `AS` 别名保持前端接口不变
2. **缓存机制**: cacheService 现在仅保存统计信息，响应内容仅在内存中
3. **字段映射**: 所有字段名已与 database.js 中的 CREATE TABLE 定义完全匹配
4. **向后兼容**: 通过别名映射确保前端代码无需修改

---

## 🎯 下一步

修复完成后，请：

1. 重启后端服务：`npm run server`
2. 测试 AI 对话功能
3. 测试模型管理功能
4. 检查数据库日志确认无错误

---

**修复完成时间**: 2026-03-31 22:50  
**修复状态**: ✅ 全部完成
