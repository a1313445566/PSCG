# 安全监控增强需求

## 📋 文档说明

**文档类型**: 需求文档  
**适用对象**: AI 开发助手  
**优先级**: P0（高优先级）  
**预计时间**: 6-8 小时  
**创建日期**: 2026-03-25  
**版本**: v3.0（修正版）

---

## 🎯 需求概述

为安全监控页面增加审计追溯功能，包括操作日志记录、封禁原因填写、批量封禁和封禁历史查询。本需求面向学校环境，需满足教育行业的审计规范要求。

---

## 📦 项目现状检查（AI 必须先阅读）

### 已有实现

#### 1. 安全监控 API（server.cjs）

```javascript
// ✅ 已存在的限流器
const { globalLimiter, apiLimiter, submitLimiter } = require('./middleware/rateLimit');

// ✅ 已存在的安全 API
app.get('/api/security/rate-limit', adminAuth, ...)
app.get('/api/security/signature-cache', adminAuth, ...)
app.post('/api/security/block-ip', adminAuth, ...)
app.post('/api/security/unblock-ip', adminAuth, ...)
app.post('/api/security/unblock-all', adminAuth, ...)
```

#### 2. 数据库实例

```javascript
// ✅ 已存在的数据库实例
const db = require('./services/database');

// Database 类提供的方法
class Database {
  async query(sql, params) { ... }
  async get(sql, params) { ... }
  async run(sql, params) { ... }
  async close() { ... }
}
```

#### 3. 限流器类（middleware/rateLimit.js）

```javascript
// ✅ RateLimiter 类提供的方法
class RateLimiter {
  getStats() { ... }
  middleware() { ... }
  manualBlock(ip, duration) { ... }
  unblock(ip) { ... }
  unblockAll() { ... }
  getClientIP(req) { ... }
}
```

---

## 🔧 技术约束（AI 必须遵守）

### 1. 数据库规范

#### ✅ 项目使用 MySQL

```javascript
// package.json
"mysql2": "^3.20.0"

// services/database.js
this.pool = mysql.createPool(config);
```

#### ✅ 必须使用 MySQL 语法

```sql
-- ❌ 错误：SQLite 语法
CREATE TABLE security_logs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  ...
);

-- ✅ 正确：MySQL 语法
CREATE TABLE IF NOT EXISTS security_logs (
  id INT AUTO_INCREMENT PRIMARY KEY,
  type VARCHAR(50) NOT NULL,
  ip VARCHAR(100) NOT NULL,
  duration INT,
  reason TEXT,
  operator VARCHAR(100) NOT NULL,
  timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,
  metadata TEXT,
  INDEX idx_timestamp (timestamp),
  INDEX idx_ip (ip),
  INDEX idx_operator (operator),
  INDEX idx_type (type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### ✅ 使用项目的 Database 实例

```javascript
// ✅ 正确的数据库操作方式
const db = require('./services/database');

// 查询单条
const row = await db.get('SELECT * FROM security_logs WHERE id = ?', [id]);

// 查询多条
const rows = await db.query('SELECT * FROM security_logs LIMIT ?', [limit]);

// 执行更新/插入
await db.run('INSERT INTO security_logs SET ?', data);
```

---

### 2. API 调用规范

#### ✅ 必须使用现有 API 封装

```javascript
// ❌ 禁止使用原生 fetch
const response = await fetch('/api/security/block-ip', {...})

// ✅ 必须使用项目的 api 封装
import { api } from '@/utils/api'

// GET 请求
const result = await api.get('/api/security/logs', { page: 1, limit: 20 })

// POST 请求
const result = await api.post('/api/security/block-ip', { 
  ip: '192.168.1.1', 
  reason: '恶意攻击' 
})

// DELETE 请求
const result = await api.delete('/api/security/logs/123')
```

#### 📦 API 封装位置
- **文件**: `src/utils/api.js`
- **功能**: 自动重试、防抖、超时、CSRF、统一错误处理

---

### 3. 消息提示规范

#### ✅ 必须使用现有消息封装

```javascript
// ❌ 禁止直接使用 Element Plus
ElMessage.success('操作成功')

// ✅ 必须使用项目的消息封装
import message from '@/utils/message'

// 成功提示
message.success('封禁成功')

// 错误提示
message.error('封禁失败，请稍后重试')

// 警告提示
message.warning('该 IP 已被封禁')

// 确认对话框
const confirmed = await message.confirm('确定要封禁此 IP 吗？', '封禁确认', {
  type: 'warning'
})

// 删除确认
const confirmed = await message.confirmDelete('此封禁记录')

// 批量操作确认
const confirmed = await message.confirmBatch(10, '封禁')

// 操作成功/失败提示
message.actionSuccess('封禁')
message.actionFailed('封禁', 'IP 地址格式不正确')
```

#### 📦 消息封装位置
- **文件**: `src/utils/message.js`

---

### 4. HOOK 使用规范

#### ✅ 必须正确使用 usePagination

```javascript
import { usePagination } from '@/composables/usePagination'
import { ref } from 'vue'

// ✅ 正确的使用方式
const logTotal = ref(0)  // 先创建 total ref
const logPagination = usePagination(20, logTotal)  // 传入 total ref

// 使用分页参数
const params = logPagination.getServerParams()  // { page: 1, limit: 20 }

// 更新总数
logTotal.value = result.total  // 直接修改 total ref

// 使用事件处理
const handleSizeChange = (size) => {
  logPagination.handleSizeChange(size)
  loadLogs()
}

const handleCurrentChange = (page) => {
  logPagination.handleCurrentChange(page)
  loadLogs()
}

// 重置分页
logPagination.reset()
```

#### ✅ 必须使用 useLoading

```javascript
import { useLoading } from '@/composables/useLoading'
import { onUnmounted } from 'vue'

const { showLoading, hideLoading, withLoading, cleanup } = useLoading()

// 方式1：手动控制
showLoading('正在加载数据...')
await fetchData()
hideLoading()

// 方式2：自动包装（推荐）
await withLoading(async () => {
  await fetchData()
}, '正在加载数据...')

// 组件卸载时清理
onUnmounted(cleanup)
```

---

### 5. 错误处理规范

#### ✅ 前端错误处理

```javascript
try {
  const result = await api.post('/api/security/block-ip', data)
  message.actionSuccess('封禁')
  await loadRateLimitStatus()
} catch (error) {
  // api 已自动显示错误，无需额外处理
  // 如需自定义错误处理：
  // message.error(`封禁失败: ${error.message}`)
}
```

#### ✅ 后端错误处理

```javascript
router.post('/block-ip', adminAuth, async (req, res) => {
  try {
    const { ip, duration, reason } = req.body
    
    // 输入验证
    if (!ip || !isValidIP(ip)) {
      return res.status(400).json({ error: 'IP 地址格式不正确' })
    }
    
    // 业务逻辑
    globalLimiter.manualBlock(ip, duration)
    
    res.json({ success: true, message: '封禁成功' })
  } catch (error) {
    console.error('[安全监控] 封禁 IP 失败:', error)
    res.status(500).json({ error: '封禁失败，请稍后重试' })
  }
})
```

---

### 6. 安全规范

#### ✅ 必须实施的安全措施

```javascript
// 1. 权限验证（后端）
router.post('/block-ip', adminAuth, async (req, res) => {
  // adminAuth 中间件已验证 JWT Token
  // req.admin 包含当前管理员信息
})

// 2. 输入验证（后端）
const isValidIP = (ip) => {
  const ipv4Regex = /^(\d{1,3}\.){3}\d{1,3}$/
  const ipv6Regex = /^([0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}$/
  return ipv4Regex.test(ip) || ipv6Regex.test(ip)
}

if (!isValidIP(ip)) {
  return res.status(400).json({ error: 'IP 地址格式不正确' })
}

// 3. XSS 防护（前端）
import { escapeHtml } from '@/utils/xss-filter'

const safeReason = escapeHtml(reason)

// 4. CSRF 防护（自动）
// api 封装已自动添加 CSRF Token
```

---

### 7. 图标导入规范

#### ✅ 必须导入 Element Plus 图标

```vue
<script setup>
// ✅ 正确的图标导入
import { Delete, Download, View } from '@element-plus/icons-vue'
</script>

<template>
  <el-icon><Delete /></el-icon>
  <el-icon><Download /></el-icon>
  <el-icon><View /></el-icon>
</template>
```

---

## 📦 需求详情

### 需求 1: 操作日志记录系统

#### 功能描述
记录所有安全监控操作（封禁、解封、批量操作），支持查询和分页。

#### 技术实现

##### 步骤 1: 数据库准备

**在 server.cjs 启动时自动创建表**

```javascript
// 在 server.cjs 的适当位置添加（例如，在数据库连接后）

// 创建操作日志表
const createSecurityLogsTable = async () => {
  const createTableSQL = `
    CREATE TABLE IF NOT EXISTS security_logs (
      id INT AUTO_INCREMENT PRIMARY KEY,
      type VARCHAR(50) NOT NULL COMMENT '操作类型: block, unblock, batch_block, unblock_all',
      ip VARCHAR(100) NOT NULL COMMENT 'IP 地址',
      duration INT COMMENT '封禁时长（毫秒）',
      reason TEXT COMMENT '封禁/解封原因',
      operator VARCHAR(100) NOT NULL COMMENT '操作人（管理员用户名）',
      timestamp DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
      metadata TEXT COMMENT '其他元数据（JSON 格式）',
      INDEX idx_timestamp (timestamp),
      INDEX idx_ip (ip),
      INDEX idx_operator (operator),
      INDEX idx_type (type)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='安全监控操作日志'
  `;
  
  try {
    await db.run(createTableSQL);
    console.log('✅ 安全监控操作日志表已准备就绪');
  } catch (error) {
    console.error('❌ 创建安全监控操作日志表失败:', error);
  }
};

// 在服务器启动时调用
createSecurityLogsTable();
```

---

##### 步骤 2: 后端 API 实现

**新增日志记录工具函数**

```javascript
// 在 server.cjs 中添加

/**
 * 记录安全监控操作日志
 * @param {Object} logData - 日志数据
 * @returns {Promise<void>}
 */
const logSecurityOperation = async (logData) => {
  const { type, ip, duration, reason, operator, metadata } = logData;
  
  const sql = `
    INSERT INTO security_logs (type, ip, duration, reason, operator, metadata)
    VALUES (?, ?, ?, ?, ?, ?)
  `;
  
  try {
    await db.run(sql, [
      type,
      ip,
      duration || null,
      reason || null,
      operator,
      metadata ? JSON.stringify(metadata) : null
    ]);
  } catch (error) {
    console.error('[安全监控] 记录操作日志失败:', error);
  }
};

// IP 验证函数
const isValidIP = (ip) => {
  const ipv4Regex = /^(\d{1,3}\.){3}\d{1,3}$/;
  const ipv6Regex = /^([0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}$/;
  return ipv4Regex.test(ip) || ipv6Regex.test(ip);
};
```

---

**新增 API 接口**

```javascript
// 在 server.cjs 中添加（在现有安全 API 之后）

/**
 * 获取操作日志（分页）
 * GET /api/security/logs
 */
app.get('/api/security/logs', adminAuth, async (req, res) => {
  try {
    const { page = 1, limit = 20, type, ip, operator } = req.query;
    
    // 构建查询条件
    let whereClause = 'WHERE 1=1';
    const params = [];
    
    if (type) {
      whereClause += ' AND type = ?';
      params.push(type);
    }
    if (ip) {
      whereClause += ' AND ip LIKE ?';
      params.push(`%${ip}%`);
    }
    if (operator) {
      whereClause += ' AND operator LIKE ?';
      params.push(`%${operator}%`);
    }
    
    // 查询总数
    const countSQL = `SELECT COUNT(*) as total FROM security_logs ${whereClause}`;
    const countResult = await db.get(countSQL, params);
    
    // 分页查询
    const offset = (page - 1) * limit;
    const logsSQL = `
      SELECT * FROM security_logs 
      ${whereClause}
      ORDER BY timestamp DESC
      LIMIT ? OFFSET ?
    `;
    const logs = await db.query(logsSQL, [...params, parseInt(limit), parseInt(offset)]);
    
    res.json({
      logs: logs || [],
      total: countResult.total,
      page: parseInt(page),
      limit: parseInt(limit)
    });
  } catch (error) {
    console.error('[安全监控] 获取操作日志失败:', error);
    res.status(500).json({ error: '获取操作日志失败' });
  }
});

/**
 * 批量封禁 IP
 * POST /api/security/batch-block
 */
app.post('/api/security/batch-block', adminAuth, async (req, res) => {
  try {
    const { ips, duration, reason } = req.body;
    
    // 输入验证
    if (!Array.isArray(ips) || ips.length === 0) {
      return res.status(400).json({ error: 'IP 列表不能为空' });
    }
    if (ips.length > 100) {
      return res.status(400).json({ error: '单次最多封禁 100 个 IP' });
    }
    if (!reason || reason.trim() === '') {
      return res.status(400).json({ error: '封禁原因不能为空' });
    }
    
    // 验证每个 IP 格式
    const invalidIPs = ips.filter(ip => !isValidIP(ip));
    if (invalidIPs.length > 0) {
      return res.status(400).json({ 
        error: `以下 IP 格式不正确: ${invalidIPs.join(', ')}` 
      });
    }
    
    // 批量封禁
    let successCount = 0;
    for (const ip of ips) {
      try {
        globalLimiter.manualBlock(ip, duration);
        successCount++;
      } catch (e) {
        console.error(`[安全监控] 封禁 IP ${ip} 失败:`, e);
      }
    }
    
    // 记录操作日志
    await logSecurityOperation({
      type: 'batch_block',
      ip: ips.join(','),
      duration,
      reason,
      operator: req.admin.username,
      metadata: { 
        total: ips.length,
        success: successCount
      }
    });
    
    res.json({ 
      success: true, 
      message: `成功封禁 ${successCount}/${ips.length} 个 IP`,
      successCount,
      totalCount: ips.length
    });
  } catch (error) {
    console.error('[安全监控] 批量封禁失败:', error);
    res.status(500).json({ error: '批量封禁失败，请稍后重试' });
  }
});
```

---

**修改现有接口**

```javascript
// 修改现有的封禁接口
app.post('/api/security/block-ip', adminAuth, async (req, res) => {
  const { ip, duration, reason } = req.body;
  
  // 输入验证
  if (!ip) {
    return res.status(400).json({ error: '缺少 IP 参数' });
  }
  if (!isValidIP(ip)) {
    return res.status(400).json({ error: 'IP 地址格式不正确' });
  }
  
  // 执行封禁
  globalLimiter.manualBlock(ip, duration || 3600000);
  
  // ✅ 记录操作日志
  await logSecurityOperation({
    type: 'block',
    ip,
    duration: duration || 3600000,
    reason: reason || '未填写',
    operator: req.admin.username
  });
  
  res.json({ success: true, message: `IP ${ip} 已被封禁` });
});

// 修改现有的解封接口
app.post('/api/security/unblock-ip', adminAuth, async (req, res) => {
  const { ip, reason } = req.body;
  
  if (!ip) {
    return res.status(400).json({ error: '缺少 IP 参数' });
  }
  
  // 执行解封
  globalLimiter.unblock(ip);
  apiLimiter.unblock(ip);
  submitLimiter.unblock(ip);
  
  // ✅ 记录操作日志
  await logSecurityOperation({
    type: 'unblock',
    ip,
    reason: reason || '手动解封',
    operator: req.admin.username
  });
  
  res.json({ success: true, message: `IP ${ip} 已解除封禁` });
});

// 修改现有的批量解封接口
app.post('/api/security/unblock-all', adminAuth, async (req, res) => {
  const globalResult = globalLimiter.unblockAll();
  const apiResult = apiLimiter.unblockAll();
  const submitResult = submitLimiter.unblockAll();
  
  // ✅ 记录操作日志
  await logSecurityOperation({
    type: 'unblock_all',
    ip: 'ALL',
    reason: '批量解封',
    operator: req.admin.username,
    metadata: { 
      global: globalResult,
      api: apiResult,
      submit: submitResult
    }
  });
  
  const operator = req.admin?.username || 'unknown';
  console.log(`🟢 [安全] 管理员 "${operator}" 已解除所有 IP 封禁`, {
    global: globalResult,
    api: apiResult,
    submit: submitResult,
    operator,
    timestamp: new Date().toISOString()
  });
  
  res.json({ 
    success: true, 
    message: '已解除所有封禁',
    details: { global: globalResult, api: apiResult, submit: submitResult }
  });
});
```

---

##### 步骤 3: 前端实现

**修改 SecurityMonitor.vue**

```vue
<template>
  <div class="security-monitor">
    <!-- 新增 Tabs -->
    <el-tabs v-model="activeTab" @tab-change="handleTabChange">
      <!-- 实时监控 Tab -->
      <el-tab-pane label="实时监控" name="realtime">
        <!-- 现有的实时监控内容 -->
        <!-- ... 保持现有代码不变 ... -->
      </el-tab-pane>
      
      <!-- 操作日志 Tab -->
      <el-tab-pane label="操作日志" name="logs">
        <!-- 筛选条件 -->
        <el-form :inline="true" :model="logFilters" class="log-filters">
          <el-form-item label="操作类型">
            <el-select v-model="logFilters.type" placeholder="全部" clearable style="width: 150px;">
              <el-option label="封禁" value="block" />
              <el-option label="解封" value="unblock" />
              <el-option label="批量封禁" value="batch_block" />
              <el-option label="批量解封" value="unblock_all" />
            </el-select>
          </el-form-item>
          <el-form-item label="IP 地址">
            <el-input v-model="logFilters.ip" placeholder="输入 IP 地址" clearable style="width: 200px;" />
          </el-form-item>
          <el-form-item label="操作人">
            <el-input v-model="logFilters.operator" placeholder="输入操作人" clearable style="width: 150px;" />
          </el-form-item>
          <el-form-item>
            <el-button type="primary" @click="loadLogs">查询</el-button>
            <el-button @click="resetLogFilters">重置</el-button>
          </el-form-item>
        </el-form>
        
        <!-- 日志表格 -->
        <el-table :data="logs" stripe v-loading="logsLoading" style="margin-top: 20px;">
          <el-table-column prop="ip" label="IP 地址" width="150" />
          <el-table-column prop="type" label="操作类型" width="120">
            <template #default="{ row }">
              <el-tag :type="getLogTypeTag(row.type)">
                {{ getLogTypeText(row.type) }}
              </el-tag>
            </template>
          </el-table-column>
          <el-table-column prop="reason" label="原因" min-width="200" show-overflow-tooltip />
          <el-table-column prop="operator" label="操作人" width="120" />
          <el-table-column prop="timestamp" label="操作时间" width="180">
            <template #default="{ row }">
              {{ formatTimestamp(row.timestamp) }}
            </template>
          </el-table-column>
          <el-table-column prop="duration" label="封禁时长" width="120">
            <template #default="{ row }">
              {{ row.duration ? formatDuration(row.duration / 1000) : '-' }}
            </template>
          </el-table-column>
        </el-table>
        
        <!-- 分页 -->
        <el-pagination
          v-model:current-page="logCurrentPage"
          v-model:page-size="logPageSize"
          :total="logTotal"
          :page-sizes="[10, 20, 50, 100]"
          layout="total, sizes, prev, pager, next, jumper"
          @size-change="handleLogSizeChange"
          @current-change="handleLogCurrentChange"
          style="margin-top: 20px; justify-content: center;"
        />
      </el-tab-pane>
    </el-tabs>
  </div>
</template>

<script setup>
import { ref, reactive, onMounted, onUnmounted } from 'vue'
import { api } from '@/utils/api'
import message from '@/utils/message'
import { useLoading } from '@/composables/useLoading'
import { formatDuration } from '@/utils/format'

// ✅ 使用 Hook
const { withLoading, cleanup } = useLoading()

// 响应式数据
const activeTab = ref('realtime')
const logs = ref([])
const logsLoading = ref(false)
const logTotal = ref(0)  // ✅ 创建 total ref
const logCurrentPage = ref(1)
const logPageSize = ref(20)

const logFilters = reactive({
  type: '',
  ip: '',
  operator: ''
})

// 加载操作日志
const loadLogs = async () => {
  try {
    logsLoading.value = true
    
    const params = {
      page: logCurrentPage.value,
      limit: logPageSize.value,
      ...logFilters
    }
    
    const result = await api.get('/api/security/logs', params)
    
    logs.value = result.logs
    logTotal.value = result.total  // ✅ 正确更新 total
  } catch (error) {
    // api 已自动显示错误
  } finally {
    logsLoading.value = false
  }
}

// 重置筛选
const resetLogFilters = () => {
  logFilters.type = ''
  logFilters.ip = ''
  logFilters.operator = ''
  logCurrentPage.value = 1
  loadLogs()
}

// 格式化操作类型
const getLogTypeText = (type) => {
  const map = {
    block: '封禁',
    unblock: '解封',
    batch_block: '批量封禁',
    unblock_all: '批量解封'
  }
  return map[type] || type
}

const getLogTypeTag = (type) => {
  const map = {
    block: 'danger',
    unblock: 'success',
    batch_block: 'warning',
    unblock_all: 'info'
  }
  return map[type] || ''
}

// ✅ 格式化时间戳
const formatTimestamp = (timestamp) => {
  return new Date(timestamp).toLocaleString('zh-CN', {
    timeZone: 'Asia/Shanghai'
  })
}

// Tab 切换
const handleTabChange = (tab) => {
  if (tab === 'logs') {
    loadLogs()
  }
}

// 分页事件
const handleLogSizeChange = (size) => {
  logPageSize.value = size
  logCurrentPage.value = 1
  loadLogs()
}

const handleLogCurrentChange = (page) => {
  logCurrentPage.value = page
  loadLogs()
}

// 生命周期
onMounted(() => {
  // 如果默认显示日志 Tab，加载日志
  if (activeTab.value === 'logs') {
    loadLogs()
  }
})

onUnmounted(cleanup)
</script>

<style scoped>
.log-filters {
  margin-bottom: 20px;
}
</style>
```

---

### 需求 2: 封禁原因填写

#### 功能描述
在手动封禁时，要求填写封禁原因。

#### 技术实现

**修改封禁对话框**

```vue
<template>
  <!-- 手动封禁对话框 -->
  <el-dialog v-model="blockDialogVisible" title="封禁 IP" width="500px">
    <el-form :model="blockForm" label-width="100px">
      <el-form-item label="IP 地址" required>
        <el-input v-model="blockForm.ip" placeholder="输入要封禁的 IP 地址" />
      </el-form-item>
      <el-form-item label="封禁时长" required>
        <el-select v-model="blockForm.duration" placeholder="选择封禁时长">
          <el-option label="10 分钟" :value="600000" />
          <el-option label="1 小时" :value="3600000" />
          <el-option label="24 小时" :value="86400000" />
          <el-option label="7 天" :value="604800000" />
          <el-option label="永久" :value="31536000000" />
        </el-select>
      </el-form-item>
      <!-- ✅ 新增：封禁原因 -->
      <el-form-item label="封禁原因" required>
        <el-input
          v-model="blockForm.reason"
          type="textarea"
          :rows="3"
          placeholder="请填写封禁原因（必填）"
          maxlength="200"
          show-word-limit
        />
      </el-form-item>
    </el-form>
    <template #footer>
      <el-button @click="blockDialogVisible = false">取消</el-button>
      <el-button type="danger" @click="handleBlock" :disabled="!blockForm.reason">
        确定封禁
      </el-button>
    </template>
  </el-dialog>
</template>

<script setup>
import { ref, reactive } from 'vue'
import { api } from '@/utils/api'
import message from '@/utils/message'
import { useLoading } from '@/composables/useLoading'
import { escapeHtml } from '@/utils/xss-filter'

const { withLoading } = useLoading()

const blockDialogVisible = ref(false)
const blockForm = reactive({
  ip: '',
  duration: 600000,
  reason: ''
})

// 封禁处理
const handleBlock = async () => {
  // 输入验证
  if (!blockForm.ip) {
    message.warning('请输入 IP 地址')
    return
  }
  if (!blockForm.reason) {
    message.warning('请填写封禁原因')
    return
  }
  
  // XSS 防护
  const safeReason = escapeHtml(blockForm.reason.trim())
  
  // 确认操作
  const confirmed = await message.confirm(
    `确定要封禁 IP ${blockForm.ip} 吗？`,
    '封禁确认',
    { type: 'warning' }
  )
  if (!confirmed) return
  
  try {
    await withLoading(async () => {
      await api.post('/api/security/block-ip', {
        ip: blockForm.ip,
        duration: blockForm.duration,
        reason: safeReason
      })
    }, '正在封禁...')
    
    message.actionSuccess('封禁')
    blockDialogVisible.value = false
    blockForm.reason = ''  // 清空原因
    
    // 刷新数据
    await loadRateLimitStatus()
  } catch (error) {
    // api 已自动显示错误
  }
}
</script>
```

---

### 需求 3: 批量封禁功能

#### 功能描述
支持批量输入多个 IP 地址进行封禁。

#### 技术实现

```vue
<template>
  <!-- 批量封禁按钮 -->
  <el-button type="danger" @click="batchBlockDialogVisible = true">
    <el-icon><Delete /></el-icon>
    批量封禁
  </el-button>
  
  <!-- 批量封禁对话框 -->
  <el-dialog v-model="batchBlockDialogVisible" title="批量封禁 IP" width="600px">
    <el-alert
      title="注意事项"
      type="warning"
      :closable="false"
      style="margin-bottom: 20px;"
    >
      <p>• 每行一个 IP 地址，最多 100 个</p>
      <p>• 支持 IPv4 和 IPv6 格式</p>
      <p>• 封禁原因必填，将应用于所有 IP</p>
    </el-alert>
    
    <el-form :model="batchBlockForm" label-width="100px">
      <el-form-item label="IP 列表" required>
        <el-input
          v-model="batchBlockForm.ipsText"
          type="textarea"
          :rows="8"
          placeholder="每行一个 IP 地址&#10;例如:&#10;192.168.1.1&#10;192.168.1.2&#10;192.168.1.3"
        />
        <div style="margin-top: 5px; color: #909399; font-size: 12px;">
          已输入 {{ ipCount }} 个 IP
        </div>
      </el-form-item>
      <el-form-item label="封禁时长" required>
        <el-select v-model="batchBlockForm.duration" placeholder="选择封禁时长">
          <el-option label="1 小时" :value="3600000" />
          <el-option label="24 小时" :value="86400000" />
          <el-option label="7 天" :value="604800000" />
          <el-option label="永久" :value="31536000000" />
        </el-select>
      </el-form-item>
      <el-form-item label="封禁原因" required>
        <el-input
          v-model="batchBlockForm.reason"
          type="textarea"
          :rows="2"
          placeholder="请填写批量封禁原因（必填）"
          maxlength="200"
          show-word-limit
        />
      </el-form-item>
    </el-form>
    
    <template #footer>
      <el-button @click="batchBlockDialogVisible = false">取消</el-button>
      <el-button 
        type="danger" 
        @click="handleBatchBlock"
        :disabled="ipCount === 0 || !batchBlockForm.reason"
      >
        确定封禁
      </el-button>
    </template>
  </el-dialog>
</template>

<script setup>
import { ref, reactive, computed } from 'vue'
import { api } from '@/utils/api'
import message from '@/utils/message'
import { useLoading } from '@/composables/useLoading'
import { escapeHtml } from '@/utils/xss-filter'
import { Delete } from '@element-plus/icons-vue'  // ✅ 导入图标

const { withLoading } = useLoading()

const batchBlockDialogVisible = ref(false)
const batchBlockForm = reactive({
  ipsText: '',
  duration: 86400000,  // 默认 24 小时
  reason: ''
})

// 计算 IP 数量
const ipCount = computed(() => {
  return batchBlockForm.ipsText
    .split('\n')
    .map(ip => ip.trim())
    .filter(ip => ip.length > 0)
    .length
})

// 批量封禁
const handleBatchBlock = async () => {
  // 解析 IP 列表
  const ips = batchBlockForm.ipsText
    .split('\n')
    .map(ip => ip.trim())
    .filter(ip => ip.length > 0)
  
  // 输入验证
  if (ips.length === 0) {
    message.warning('请输入至少一个 IP 地址')
    return
  }
  if (ips.length > 100) {
    message.warning('单次最多封禁 100 个 IP')
    return
  }
  if (!batchBlockForm.reason) {
    message.warning('请填写封禁原因')
    return
  }
  
  // XSS 防护
  const safeReason = escapeHtml(batchBlockForm.reason.trim())
  
  // 确认操作
  const confirmed = await message.confirmBatch(ips.length, '封禁')
  if (!confirmed) return
  
  try {
    const result = await withLoading(async () => {
      return await api.post('/api/security/batch-block', {
        ips,
        duration: batchBlockForm.duration,
        reason: safeReason
      })
    }, '正在批量封禁...')
    
    message.success(result.message)
    batchBlockDialogVisible.value = false
    batchBlockForm.ipsText = ''
    batchBlockForm.reason = ''
    
    // 刷新数据
    await loadRateLimitStatus()
  } catch (error) {
    // api 已自动显示错误
  }
}
</script>
```

---

## ✅ 验收标准

### 功能验收

- [ ] **操作日志记录**
  - [ ] 所有封禁操作自动记录到数据库
  - [ ] 所有解封操作自动记录到数据库
  - [ ] 日志包含完整信息：IP、类型、原因、操作人、时间
  - [ ] 支持分页查询
  - [ ] 支持筛选（类型、IP、操作人）

- [ ] **封禁原因填写**
  - [ ] 手动封禁必须填写原因
  - [ ] 原因字段必填验证
  - [ ] 原因字段长度限制（200 字符）
  - [ ] XSS 防护（使用 escapeHtml）

- [ ] **批量封禁**
  - [ ] 支持批量输入 IP（每行一个）
  - [ ] 单次最多 100 个 IP
  - [ ] IP 格式验证
  - [ ] 封禁原因必填
  - [ ] 显示成功/失败统计

### 技术验收

- [ ] **数据库规范**
  - [ ] 使用 MySQL 语法（AUTO_INCREMENT）
  - [ ] 使用项目 Database 实例
  - [ ] 索引已创建

- [ ] **API 规范**
  - [ ] 使用 `api.get/post/delete` 封装
  - [ ] 所有 API 使用 adminAuth 中间件
  - [ ] 所有 API 包含错误处理

- [ ] **HOOK 使用**
  - [ ] 正确使用 useLoading Hook
  - [ ] 正确使用 usePagination（total 作为参数）
  - [ ] 组件卸载时调用 cleanup

- [ ] **消息提示**
  - [ ] 使用 message.success/error/warning
  - [ ] 使用 message.confirm/confirmBatch

- [ ] **安全措施**
  - [ ] XSS 防护（escapeHtml）
  - [ ] 输入验证（IP 格式、必填字段）
  - [ ] 权限验证（adminAuth）

- [ ] **图标导入**
  - [ ] 所有图标已正确导入
  - [ ] 使用 @element-plus/icons-vue

### 性能验收

- [ ] 日志查询响应时间 < 1 秒
- [ ] 批量封禁 100 个 IP < 3 秒
- [ ] 数据库索引已创建

---

## 🚀 实施步骤

### 第一步：数据库准备（10 分钟）
1. 在 server.cjs 添加建表 SQL
2. 启动服务器验证表已创建
3. 检查数据库索引

### 第二步：后端 API（60 分钟）
1. 添加日志记录工具函数
2. 添加 IP 验证函数
3. 新增批量封禁接口
4. 新增日志查询接口
5. 修改现有封禁/解封接口
6. 测试所有 API

### 第三步：前端实现（90 分钟）
1. 修改 SecurityMonitor.vue 添加日志 Tab
2. 实现日志查询和筛选
3. 修改封禁对话框（添加原因字段）
4. 新增批量封禁对话框
5. 集成所有功能
6. 测试所有功能

### 第四步：测试验证（30 分钟）
1. 功能测试
2. 安全测试
3. 性能测试

---

## 📝 注意事项

### 必须遵守的规矩

1. **禁止使用 SQLite 语法** - 必须使用 MySQL 语法
2. **禁止使用原生 fetch** - 必须使用 `api` 封装
3. **禁止直接使用 Element Plus 消息** - 必须使用 `message` 封装
4. **禁止错误使用 usePagination** - total 作为参数传入，不是返回值
5. **禁止忘记导入图标** - 所有图标必须从 `@element-plus/icons-vue` 导入
6. **禁止绕过权限验证** - 所有 API 必须使用 adminAuth
7. **禁止忽略错误处理** - 所有异步操作必须 try-catch
8. **禁止在生产环境输出调试日志** - 仅保留错误日志

### 增量开发原则

1. **不破坏现有功能** - 所有修改向后兼容
2. **逐步验证** - 每个功能完成后立即测试
3. **保留旧代码** - 新增文件，不删除现有文件
4. **可回滚** - 每个步骤独立提交，方便回滚

---

## 📚 参考资料

- 数据库实例: `services/database.js`
- API 封装: `src/utils/api.js`
- 消息封装: `src/utils/message.js`
- Loading Hook: `src/composables/useLoading.js`
- 分页 Hook: `src/composables/usePagination.js`
- XSS 防护: `src/utils/xss-filter.js`
- 格式化工具: `src/utils/format.js`
- 权限验证: `middleware/adminAuth.js`
- 限流器: `middleware/rateLimit.js`

---

**文档版本**: v3.0（修正版）  
**最后更新**: 2026-03-26  
**状态**: 已完成
