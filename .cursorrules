# PSCG 智能题库系统 - 项目规则

## 项目概述
PSCG (Primary School Comprehensive Quiz) 是一个基于 Vue 3 + Express + MySQL 的智能题库系统，支持多学科题库管理、智能难度调整、答题统计分析和 AI 智能分析功能。

## 技术栈

### 前端
- **框架**: Vue 3 + Composition API
- **状态管理**: Pinia (`defineStore`)
- **路由**: Vue Router 4
- **UI组件库**: Element Plus
- **图表**: VChart + ECharts
- **富文本**: Quill
- **Markdown**: marked
- **构建工具**: Vite 5

### 后端
- **运行时**: Node.js 16+
- **框架**: Express 4.x
- **数据库**: MySQL 8.0 (mysql2)
- **文件上传**: Multer
- **进程管理**: PM2
- **AI 服务**: 豆包 API

## 目录结构
```
PSCG/
├── src/                    # 前端源码
│   ├── views/              # 页面组件
│   ├── components/         # 通用组件
│   │   ├── admin/          # 后台管理组件
│   │   │   ├── ai/         # AI 分析组件
│   │   │   ├── analysis/   # 数据分析组件
│   │   │   ├── basic-settings/ # 基础设置组件
│   │   │   ├── common/     # 公共组件
│   │   │   ├── layout/     # 布局组件
│   │   │   ├── question/   # 题目管理组件
│   │   │   └── user-data/  # 用户数据组件
│   │   ├── quiz/           # 答题组件
│   │   └── common/         # 公共组件
│   ├── stores/             # Pinia 状态管理
│   ├── composables/        # 组合式函数 (Hooks)
│   │   ├── usePagination.js    # 分页 Hook
│   │   ├── useLoading.js       # 加载状态 Hook
│   │   ├── useUserFilters.js   # 用户筛选 Hook
│   │   └── useAdminLayout.js   # 后台布局 Hook
│   ├── utils/              # 工具函数
│   │   ├── api.js          # API 封装客户端
│   │   ├── message.js      # 消息提示封装
│   │   ├── format.js       # 格式化工具
│   │   ├── dateUtils.js    # 日期处理
│   │   ├── chartGenerator.js  # 图表生成
│   │   ├── markdown.js     # Markdown 渲染
│   │   ├── apiCache.js     # API 缓存工具
│   │   ├── csrf.js         # CSRF 防护
│   │   ├── xss-filter.js   # XSS 防护
│   │   ├── database.js     # 数据库配置
│   │   ├── draftStorage.js # 草稿存储
│   │   ├── imageUpload.js  # 图片上传
│   │   ├── lazyLoad.js     # 懒加载
│   │   └── shuffleOptions.js  # 选项打乱
│   ├── router/             # 路由配置
│   └── styles/             # 全局样式
│       ├── global.css      # 全局基础样式
│       ├── common.css      # 公共样式
│       ├── loading.css     # 加载样式
│       └── markdown.css    # Markdown 样式
├── routes/                 # 后端 API 路由
│   ├── ai.js               # AI 分析接口
│   ├── analysis.js         # 数据分析接口
│   ├── dashboard.js        # 仪表盘接口
│   ├── questions.js        # 题目管理接口
│   ├── quiz.js             # 答题接口
│   ├── users.js            # 用户管理接口
│   └── ...                 # 其他路由
├── services/               # 后端服务层
│   ├── aiService.js        # AI 服务
│   ├── database.js         # 数据库服务
│   ├── difficultyService.js # 难度服务
│   ├── validationService.js # 验证服务
│   ├── cache.js            # 缓存服务
│   └── passwordHash.js     # 密码加密
├── middleware/             # Express 中间件
│   ├── csrf.js             # CSRF 防护
│   ├── adminAuth.js        # 管理员认证
│   ├── rateLimit.js        # 速率限制
│   ├── dbPerformance.js    # 数据库性能监控
│   ├── responseTime.js     # 响应时间记录
│   └── signatureCache.js   # 签名缓存
├── utils/                  # 后端工具函数
│   ├── format.js           # 格式化工具 (CommonJS)
│   └── xss-filter.js       # XSS 防护
├── config/                 # 配置文件
├── DOCS/                   # 项目文档
│   ├── 需求文档/           # 需求相关
│   ├── 技术文档/           # 技术文档
│   ├── API文档/            # API 接口文档
│   ├── 开发文档/           # 开发规范
│   └── AI辅助开发指南.md   # AI 开发指南
├── audio/                  # 音频资源
├── images/                 # 图片资源
├── server.cjs              # 后端入口
└── vite.config.js          # Vite 配置
```

## 编码规范

### Vue 组件
- 使用 `<script setup>` 语法糖
- 组件命名使用 PascalCase (如 `QuestionCard.vue`)
- Props 定义使用 `defineProps`，Emits 使用 `defineEmits`
- 使用 Composition API 的 `ref`、`reactive`、`computed`

### Pinia Store
- 使用 `defineStore` 创建 store
- Store 命名: `useXxxStore` (如 `useQuestionStore`)
- 结构: `state`、`getters`、`actions`
- 异步操作使用 `async/await`
- 错误向上抛出，让调用者统一处理

### Composables (Hooks)
- 文件命名: `src/composables/useXxx.js`
- 异步操作必须有 `try-catch-finally`
- `onUnmounted` 清理定时器、事件监听、网络请求
- 错误向上抛出: `throw error` 让调用者处理
- 日志带前缀: `console.error('[useXxx] 错误:', error)`

### 后端代码
- 使用 CommonJS (`require`/`module.exports`)
- 路由文件放在 `routes/` 目录
- 数据库操作放在 `services/` 目录
- 中间件放在 `middleware/` 目录
- 工具函数放在 `utils/` 目录 (CommonJS 格式)

### API 规范
- 基础路径: `/api`
- RESTful 风格
- 响应格式: JSON
- 错误处理: `{ error: '错误信息' }`

### 样式规范
- 全局样式: `src/styles/global.css`
- 公共样式: `src/styles/common.css`
- 组件样式使用 `<style scoped>`
- 使用 Element Plus 主题变量

### 文档规范
- **需求文档**: `DOCS/需求文档/`
- **技术文档**: `DOCS/技术文档/`
- **API 文档**: `DOCS/API文档/`
- **开发文档**: `DOCS/开发文档/`
- **命名**: 使用清晰的中文或英文命名
- **避免重复**: 整合相关内容，删除过时文档

## 常用命令

```bash
# 开发
npm run dev          # 启动前端开发服务器 (端口 5173)
npm run server       # 启动后端服务器 (端口 3001)

# 构建
npm run build        # 构建生产版本
npm run preview      # 预览生产构建

# 进程管理
pm2 start server.cjs --name "pscg-app"
pm2 logs pscg-app
pm2 restart pscg-app
pm2 restart 0        # 重启 ID 为 0 的进程
```

## 部署流程

```bash
# 1. 静态构建
npm run build

# 2. 重启服务器
pm2 restart 0
```

## 环境配置
- 开发环境: `.env.development`
- 生产环境: `.env.production`
- API 地址通过 `VITE_API_URL` 配置

## 安全措施
- **XSS 防护**: `src/utils/xss-filter.js` + `utils/xss-filter.js`
- **CSRF 防护**: `middleware/csrf.js` + `src/utils/csrf.js`
- **速率限制**: `middleware/rateLimit.js`
- **输入验证**: 前后端双重验证

## 注意事项
- 后端文件使用 `.cjs` 扩展名 (CommonJS)
- 前端使用 ES Modules (`import`/`export`)
- 数据库连接使用连接池
- 静态资源缓存已配置
- 文件上传限制 10MB
- AI 分析超时 90 秒

## 🔴 编码前必做检查

```
1. 搜索项目中是否已有相关工具方法
   - src/composables/  目录下的 useXxx.js
   - src/utils/        目录下的工具函数
   - src/stores/       目录下的 Store
   - utils/            目录下的后端工具 (CommonJS)

2. 没有才新建，有则复用
```

## 🔴 强制性规则

### 前端
| 规则 | 说明 |
|------|------|
| async 必须 try-catch | 所有异步函数必须有错误处理 |
| API 用 api.js | 使用 `src/utils/api.js` 封装客户端 |
| 消息用 message.js | 使用 `src/utils/message.js` 统一提示 |
| 格式化用 format.js | 使用 `src/utils/format.js` 工具函数 |
| 路由懒加载用 lazyLoad | `lazyLoad('XxxView.vue')` + `.catch()` |
| 异步组件完整配置 | `defineAsyncComponent({ loader, loadingComponent, errorComponent, timeout })` |
| Store actions 有 error | `this.error = error.message` + 向上抛出 |
| fetch 检查 response.ok | 处理非 200 响应 |

### 后端
| 规则 | 说明 |
|------|------|
| 路由 try-catch | 所有 `async (req, res)` 必须有错误处理 |
| 参数化查询 | 防止 SQL 注入 |
| 参数验证优先 | 验证在业务逻辑之前 |
| 事务有 rollback | 失败时回滚 |
| 格式化用 format.js | 使用 `utils/format.js` (CommonJS) |

### Composables (Hooks)
| 规则 | 说明 |
|------|------|
| 文件命名 | `src/composables/useXxx.js` |
| 异步操作 try-catch-finally | 必须有错误处理 |
| onUnmounted 清理 | 定时器、事件监听、网络请求必须清理 |
| 错误向上抛出 | `throw error` 让调用者处理 |
| 日志带前缀 | `console.error('[useXxx] 错误:', error)` |

## 📋 标准模板

### Composable 模板
```javascript
import { ref, onUnmounted } from 'vue'
import { api } from '@/utils/api'
import message from '@/utils/message'

export function useXxx(options = {}) {
  const data = ref(null)
  const isLoading = ref(false)
  const error = ref(null)
  let timer = null

  const execute = async (params) => {
    isLoading.value = true
    try {
      const result = await api.get('/endpoint', { params })
      data.value = result
      return result
    } catch (err) {
      error.value = err.message
      console.error('[useXxx] 失败:', err)
      throw err
    } finally {
      isLoading.value = false
    }
  }

  const cleanup = () => { if (timer) clearTimeout(timer) }
  onUnmounted(cleanup)

  return { data, isLoading, error, execute, cleanup }
}
```

### 后端路由模板
```javascript
router.get('/', async (req, res) => {
  try {
    const data = await db.all('SELECT * FROM table')
    res.json(data)
  } catch (error) {
    console.error('操作失败:', error)
    res.status(500).json({ error: '操作失败' })
  }
})
```

### 异步组件模板
```javascript
defineAsyncComponent({
  loader: () => import('./Component.vue'),
  loadingComponent: { template: '<div>加载中...</div>' },
  errorComponent: { template: '<div>加载失败</div>' },
  delay: 200,
  timeout: 10000
})
```

## 📚 详细规范

详见：`DOCS/开发文档/编码规范.md`
      `DOCS/AI辅助开发指南.md`
